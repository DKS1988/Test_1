%h1 Add test

%p
  %b Name:
  = @group.name

= form_tag save_test_group_path do

  .field
    = select_tag 'test', options_from_collection_for_select(@tests, "id", "name")
  .actions
    = submit_tag 'Save'

= link_to 'Show', @group
\|
= link_to 'Back', group_path
