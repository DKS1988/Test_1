%h1 Add course

%p
  %b Name:
  = @group.name

= form_tag save_course_group_path do

  .field
    = select_tag 'course', options_from_collection_for_select(@courses, "id", "name")
  .actions
    = submit_tag 'Save'

= link_to 'Show', @group
\|
= link_to 'Back', group_path
